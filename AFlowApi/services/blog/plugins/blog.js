const Util = require('../../util');
const Boom = require('boom');

module.exports = function (options) {

    this.add('role:blog,cmd:statistics', async (args, respond) => {

    });

    return 'blog';
};
